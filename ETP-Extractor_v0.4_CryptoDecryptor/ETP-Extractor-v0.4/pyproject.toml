[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "etp-extractor"
version = "0.1.0"
description = "Local analyzer and extractor for authorized P&WC ETP offline content"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "beautifulsoup4>=4.12.3",
    "lxml>=5.2.2",
    "pydantic>=2.8.2",
    "openpyxl>=3.1.5",
    "typer>=0.12.3",
    "rich>=13.7.1"
]

[project.scripts]
etp-extractor = "etp_extractor.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
